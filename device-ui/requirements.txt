# UI Framework
kivy>=2.3.1
kivy-garden>=0.1.5

# HTTP Client for Backend API
httpx>=0.27.0
websockets>=12.0

# Async support
aiofiles>=23.2.1

# Image handling
Pillow>=10.2.0

# Audio capture (mic test + in-process audio_capture.py child)
numpy>=1.24.0
sounddevice>=0.4.6
vosk>=0.3.45
# Real mic capture + VAD pipeline (formerly in mini-pc/audio/requirements.txt;
# pulled in here so the merged device-ui image can also spawn audio_capture.py).
pyaudio==0.2.14
# webrtcvad: the original sdist needs a C compiler. On Linux we keep the
# canonical package; on Windows there is no prebuilt wheel for modern Python,
# so use webrtcvad-wheels (a drop-in providing the same ``webrtcvad`` module).
webrtcvad==2.0.10; sys_platform != "win32"
webrtcvad-wheels>=2.0.14; sys_platform == "win32"
pyyaml>=6.0.1
# webrtcvad needs pkg_resources at runtime; bare slim Python 3.12+ images drop it.
setuptools>=69.0.0

# Windows-native text-to-speech fallback (SAPI5 via pyttsx3) used when the
# Linux TTS engines (espeak-ng/piper/mimic3) are unavailable.
pyttsx3>=2.90; sys_platform == "win32"

# QR Code generation
qrcode>=7.4.2

# Date/time formatting
python-dateutil>=2.8.2
tzdata>=2024.1

# Appliance resource metrics (dashboard System page)
psutil>=5.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.5
